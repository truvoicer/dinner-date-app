import React from 'react';

const FullWidthSectionHeader = ({children}) => {
    return (
        <>
            <div className="section-header">
                {children}
            </div>
        </>
    );
};

export default FullWidthSectionHeader;
